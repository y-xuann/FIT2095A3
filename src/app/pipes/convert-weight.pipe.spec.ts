import { ConvertWeightPipe } from './convert-weight.pipe';

describe('ConvertWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
