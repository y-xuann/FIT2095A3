import { ConvertUpperPipe } from './convert-upper.pipe';

describe('ConvertUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
